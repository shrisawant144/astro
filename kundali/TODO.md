# Kundali Bug Fixes TODO
Generated by BLACKBOXAI - Fixing critical errors, key mismatches, logical issues.

## Plan Overview
- Fix runtime KeyErrors from mismatched dict keys between main.py result and consumers.
- Add safety checks for missing data.
- Correct astrological logic (double transit targets, dusthana houses).
- Compute missing D9 lagna.
- Test full pipeline.

Status: 3/11 COMPLETE

## Step-by-Step Tasks

### 1. ✅ Create this TODO.md [AI DONE]

### 2. ✅ Fix marriage_date_prediction.py key mismatches [AI DONE]

### 3. ✅ Fix dosha_detection.py D9 key [AI DONE]

### 4. ✅ Add safety & logic fixes to spouse/analysis.py [AI DONE]
### 5. ✅ Compute D9 lagna & validate birth data in spouse/predictor.py [AI DONE]

### 2. Fix marriage_date_prediction.py key mismatches
- Change kundali["lord7"] → kundali.get("lord7_full", "")
- Add: birth_date = kundali.get("birth_datetime", datetime.now()).date() if "birth_datetime" in kundali else None
- Change "dasha_periods" → "dasha_periods_for_marriage"
- File: kundali/marriage_date_prediction.py

### 3. Fix dosha_detection.py D9 key
- result.get("d9", {}) → result.get("navamsa", {})
- File: kundali/dosha_detection.py

### 4. Add safety to spouse/analysis.py dasha calcs
- md["start_jd"] → md.get("start_jd", birth_jd)
- md["end_jd"] → md.get("end_jd", birth_jd)
- Upcoming: use JD: p.get("start_jd", 0) > current_jd (compute current_jd = datetime_to_jd(datetime.now(pytz.utc)))

### 5. Fix double_transit targets in spouse/analysis.py
- targets = [1, h7_num, h7_lord_house] → targets = [h7_num, h7_lord_house]; if ul_house: targets.append(ul_house)

### 6. Fix dusthana in spouse/analysis.py analyze_d60_karma
- Remove hardcoded dusthana_signs
- Add: dk_d60_house = _get_house_d60(chart_data, "DK_planet", d60_lagna_idx) but compute d60_lagna if missing
- Proxy: if dk_d60_sign in relative dusthanas from lagna (simplified)

### 7. Compute D9 lagna in spouse/predictor.py
- Import get_navamsa_sign_and_deg from utils
- self.d9_lagna_sign = get_navamsa_sign_and_deg(self.data["lagna_deg"])[0]

### 8. Validate birth data in spouse/predictor.py _build_prediction
- if self.data.get("birth_jd", 0) == 0: pred["timing_warning"] = "Birth JD missing, timing analysis approximate"

### 9. Minor: utils.py has get_navamsa_sign_and_deg? Confirm/add if needed

### 10. Test fixes
- Run: cd kundali && python -c "from main import calculate_kundali; r=calculate_kundali('1990-01-01','12:00','Mumbai, India'); from spouse_predictor import AdvancedSpousePredictor; p=AdvancedSpousePredictor(r); print('SUCCESS')"
- Check no KeyErrors, logical outputs.

### 11. Update TODO.md [mark complete] & attempt_completion

Next step: Confirm which to do first?

